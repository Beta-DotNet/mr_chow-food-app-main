{
  "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput",
  "outputDirectory": ".",
  "framework": "django",
  "env": {
    "DJANGO_SETTINGS_MODULE": "mr_chow_proj.settings",
    "VERCEL": "1"
  }
}
